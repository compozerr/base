{
    "scripts": {
        "install": "(npm list -g ts-node || npm install -g ts-node) > /dev/null 2>&1 && ts-node scripts/install.ts",
        "dev": "deno task dev",
        "deploy": "nbgv tag && git push origin --tags"
    }
}
