{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Backend Process",
            "type": "coreclr",
            "request": "attach",
            "processName": "Api",
            "preLaunchTask": "Run npm run dev",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}